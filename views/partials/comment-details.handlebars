<section class="row">
  <div class="col-12 col-lg-12">
    <div class="card">
      <p>By: {{user_created}} On: {{format_date date_created}}</p>
      <div class="card-body">
        <p>{{description}}</p>
      </div>
    </div>
  </div>
</section>